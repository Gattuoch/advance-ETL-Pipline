# astro-sdk-python requires specific version compatibility
# Check compatibility with astro-runtime:11.7.0
astro-sdk-python==1.8.1

# duckdb and related packages
duckdb==1.0.0
airflow-provider-duckdb==0.2.0
minio==7.2.7

# Processing libraries
pyspark==3.5.1
pandas<2.2.0
numpy==1.26.4

# dbt (BONUS) - note: dbt-duckdb might have compatibility issues
dbt-core==1.8.7
dbt-duckdb==1.8.1

# Add any other dependencies you need
